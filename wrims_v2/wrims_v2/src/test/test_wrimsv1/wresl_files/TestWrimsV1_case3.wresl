! local svar is leaked into second cycle

SEQUENCE CYCLE1 {
    model   first
    order   1
    }

SEQUENCE CYCLE2 {
    model   second
    order   2
    }	
	
MODEL first { 

	define[local] x { std kind 'cycle1_local' units 'CFS' } 
	define[local] y { value 7 } 
}

MODEL second { 

	goal goal_1 { x + y < 99 }
}